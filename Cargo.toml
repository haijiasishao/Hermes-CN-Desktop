[package]
name = "hermes-agent-cn-desktop"
version = "0.7.0"
edition = "2021"
description = "Hermes Agent CN Desktop (Tauri) — Android port"
authors = ["Hermes Agent CN Contributors"]
homepage = "https://hermesagent.org.cn"

[lib]
name = "hermes_agent_cn"
# cdylib required for Tauri v2 Android (produces libhermes_agent_cn.so);
# rlib keeps desktop tests/benchmarks working.
crate-type = ["cdylib", "rlib"]

[features]
default = []
desktop = ["tauri/tray-icon", "dep:portable-pty", "dep:notify", "dep:open", "dep:fs2", "dep:tauri-plugin-notification", "dep:tauri-plugin-clipboard-manager", "dep:bytes", "dep:dotenvy", "dep:getrandom", "dep:http-body-util", "dep:hyper", "dep:hyper-util"]
android = ["dep:tauri-plugin-notification"]

[dependencies]
tauri = { version = "2", features = ["devtools"] }
tauri-plugin-dialog = "2"
tauri-plugin-fs = "2"
tauri-plugin-notification = { version = "2", optional = true }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
serde_yaml = "0.9"
tokio = { version = "1", features = ["full"] }
reqwest = { version = "0.12", default-features = false, features = [
    "cookies",
    "json",
    "multipart",
    "rustls-tls",
    "stream",
] }
cookie = "0.18"
sha2 = "0.10"
ed25519-dalek = { version = "2", features = ["pkcs8", "pem"] }
zip = "2"
dirs = "6"
regex = "1"
log = "0.4"
env_logger = "0.11"
tempfile = "3"
base64 = "0.22"
url = "2"
urlencoding = "2"
open = { version = "5", optional = true }
# Native filesystem watch powering the right-rail preview's "文件实时刷新"
# (commands/preview.rs). Raw change events are emitted to the renderer, which
# debounces before re-reading. Not available on Android.
notify = { version = "6", optional = true }
futures-util = "0.3"
# Loopback-only HTTP/WebSocket server used by the browser companion mode.
# Not needed on Android (remote mode only).
bytes = { version = "1", optional = true }
http-body-util = { version = "0.1", optional = true }
hyper = { version = "1", features = ["http1", "server"], optional = true }
hyper-util = { version = "0.1", features = ["tokio"], optional = true }
# WebSocket relay to the runtime's native /api/ws (commands/ws_proxy.rs).
# The relay also carries remote-mode connections (src/connection.rs), which
# may be wss://, so a rustls backend is required.
tokio-tungstenite = { version = "0.29", features = ["rustls-tls-webpki-roots"] }
fs2 = { version = "0.4", optional = true }
thiserror = "2"
portable-pty = { version = "0.9", optional = true }
rusqlite = { version = "0.32", features = ["bundled"] }
getrandom = { version = "0.3", optional = true }
# Parse $HERMES_HOME/.env for per-spawn child env injection (src/env_file.rs).
# Only from_path_iter is used — the desktop process env is never mutated.
dotenvy = { version = "0.15", optional = true }
tauri-plugin-clipboard-manager = { version = "2.3.2", optional = true }

[target.'cfg(target_os = "windows")'.dependencies]
windows-sys = { version = "0.61", features = [
    "Win32_Foundation",
    "Win32_Security",
    "Win32_System_JobObjects",
    "Win32_System_Threading",
] }
# Read-only registry access for live PATH/PATHEXT resolution (path_resolver.rs).
winreg = "0.55"

[target.'cfg(target_os = "macos")'.dependencies]
objc2 = "0.6"
objc2-foundation = { version = "0.3", features = ["NSProcessInfo", "NSString"] }

[dev-dependencies]
pretty_assertions = "1"
serial_test = "3"
wiremock = "0.6"

[build-dependencies]
tauri-build = { version = "2", features = [] }

[profile.release]
strip = true
lto = true
codegen-units = 1
